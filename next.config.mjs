/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/tree',
  assetPrefix: '/tree',
  reactStrictMode: true,
  // 기존 monster-tree에 있던 다른 설정이 있다면 여기에 그대로 유지
};

export default nextConfig;
